{
  "name": "password_generator",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "tsc --noEmit && npm run transpile && parcel index.html --open",
    "build": "tsc --noEmit && npm run transpile && parcel build index.html -d build --public-url .",
    "transpile": "node-sass ./src/styles.scss ./src/styles.css",
    "deploy": "gh-pages -d build",
    "predeploy": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Max Razhnov",
  "license": "MIT",
  "devDependencies": {
    "gh-pages": "^2.2.0",
    "parcel-bundler": "^1.12.4",
    "sass": "^1.23.3",
    "typescript": "^3.6.4"
  },
  "dependencies": {
    "node-sass": "^4.13.1"
  }
}
