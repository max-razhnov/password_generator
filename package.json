{
  "name": "password_generator",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm run typecheck && parcel index.html --open",
    "build": "npm run typecheck && parcel build index.html -d build",
    "typecheck": "tsc --noEmit",
    "deploy": "gh-pages -d build",
    "predeploy": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Max Razhnov",
  "license": "MIT",
  "devDependencies": {
    "gh-pages": "^2.2.0",
    "parcel-bundler": "^1.12.4",
    "sass": "^1.23.3",
    "typescript": "^3.6.4"
  },
  "dependencies": {}
}
